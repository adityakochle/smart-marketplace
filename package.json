{
  "name": "smart-marketplace",
  "version": "1.0.0",
  "description": "AI-powered marketplace connecting users with tradesmen using Zeroentropy, Arcade, and Datalog",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "render-postbuild": "npm run install-client && npm run build"
  },
  "keywords": [
    "ai",
    "marketplace",
    "tradesmen",
    "zeroentropy",
    "arcade",
    "datalog"
  ],
  "author": "MCP Agents Hackathon",
  "license": "ISC",
  "dependencies": {
    "@arcadeai/arcadejs": "^1.9.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
